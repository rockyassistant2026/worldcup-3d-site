# /studio/content

Owner: `content-seo` (HERMES MVP v1.0 spec, Agent 7).

Copy, messaging, positioning, and SEO/metadata planning docs land here. Technical SEO
changes to actual files (meta tags, structured data, sitemap) still live in application
code (`index.html`, `public/`) and are implemented via `frontend-architect`/`claude-task`.
