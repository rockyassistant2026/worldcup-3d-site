# /studio/infrastructure

Owner: `backend-infra` (HERMES MVP v1.0 spec, Agent 6).

This directory is intentionally empty right now — the project has no backend, API, CMS,
or auth. It exists so there's a home for infrastructure docs the moment a real need shows
up, not so infrastructure gets invented speculatively.
