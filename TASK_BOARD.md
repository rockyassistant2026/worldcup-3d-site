# Task Board

The real task tracker is Hermes's own kanban, not this file — this is a pointer, not a
duplicate.

**Live board: `football-3d`** (`hermes kanban --board football-3d list`).

**Archived: `3d-website`** — held 4 tasks from initial team scaffolding
(`t_1f39ebf9`, `t_55b61605`, `t_b70c7282`, `t_d16efc1c`) that were never executed and are
superseded by the real work already completed on `football-3d`. Archived, not deleted.

For the task body template (OBJECTIVE / FILES / ACCEPTANCE CRITERIA / TEST METHOD) and
role-prefix convention (`CD`, `UX`, `MO`, `FE`, `3D`, `BE`, `CO`, `QA`), see
`AGENT_RULES.md`.
